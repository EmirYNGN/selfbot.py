module.exports = member => { let guild = member.guild; let modlog = guild.channels.find('name', 'kayitlar'); let kanal = guild.channels.find('name', 'kayitlar'); let username = member.user.username; if (!modlog) return; if (!kanal) return; modlog.send(Sunucuya bir üye katıldı. ${username} 👋); const hojgeldin = new Discord.RichEmbed() .setThumbnail(member.user.avatarURL) .setDescription(${username} adlı kullanıcı sunucuya geldi!) kanal.send(hojgeldin); }

module.exports = member => { let guild = member.guild; let modlog = guild.channels.find('name', 'kayitlar'); let kanal = guild.channels.find('name', 'kayitlar'); let username = member.user.username; if (!modlog) return; if (!kanal) return; modlog.send(Sunucudan bir üye ayrıldı. ${username} 👋); const ayrildi = new Discord.RichEmbed() .setThumbnail(member.user.avatarURL) .setDescription(${username} adlı kullanıcı sunucudan ayrıldı!) kanal.send(ayrildi); }
